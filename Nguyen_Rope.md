# Rope — структура данных для работы с большими строками

## 1. Общая идея Rope

Rope — это структура данных, которая хранит строку не как один массив символов, а как бинарное дерево. В листьях дерева находятся последовательные части строки, а во внутренних вершинах не хранится сама строка — только ссылки на поддеревья и число weight, равное сумме длин всех строк в левом поддереве.

Изначально Rope может состоять из одной вершины, в которой хранится вся строка. Используя значения weight во внутренних вершинах, можно эффективно получать символы строки по индексу и выполнять другие операции.

![](https://upload.wikimedia.org/wikipedia/commons/8/8a/Vector_Rope_example.svg)

(На картинке показан пример Rope, где числа в вершинах отражают размеры поддеревьев. В данной работе под weight понимается вес левого поддерева, что используется при навигации по индексу.)




---

## 2. Обозначения

Будем использовать следующие обозначения:

- n — длина всей строки  
- h — высота дерева  

Если дерево сбалансировано, то h = O(log n).

---

## 3. Основные операции

### 3.1. Доступ к символу по индексу (get)

Чтобы получить символ с индексом i, алгоритм начинает в корне дерева. Если текущая вершина не является листом, сравнивается i и вес левого поддерева. Если вес левого поддерева больше либо равен i, переход происходит в левое поддерево. Иначе переход происходит в правое поддерево, но уже с индексом i - weight. Если текущая вершина — лист, то нужный символ берётся из строки, которая хранится в этом листе.

Псевдокод:

    function GET(node, i):
        if node.left is not null:
            if node.left.w >= i:
                return GET(node.left, i)
            else:
                return GET(node.right, i - node.left.w)
        else:
            return node.string[i]


**Так как алгоритм на каждом шаге спускается на один уровень ниже, время работы операции get равно O(h).**

---

### 3.2. Конкатенация строк (merge)
![](https://neerc.ifmo.ru/wiki/images/3/3a/String_concat.png)
Для конкатенации двух Rope создаётся новая вершина, у которой левым сыном становится первое дерево, а правым — второе. Вес новой вершины равен сумме длин строк в левом поддереве.

Псевдокод:

    function MERGE(n1, n2):
        return Node(n1, n2, n1.w + n2.w)

**Так как при конкатенации создаётся ровно одна новая вершина, время работы операции merge равно O(1).**


---

### 3.3. Разделение строки (split)

Операция split(i) разбивает строку на две: первую с индексами [0, i) и вторую с индексами [i, n). Алгоритм работает похожим образом на get: он спускается по дереву, а на пути делит вершины на две части и пересчитывает веса.

Если текущая вершина — лист, строка в ней просто делится на две подстроки. Если вершина внутренняя, рекурсивно выполняется split для нужного поддерева, после чего собираются два новых дерева.

Псевдокод:

    function SPLIT(node, i):
        if node.left is not null:
            if node.left.w >= i:
                (t1, t2) = SPLIT(node.left, i)
                tree1 = t1
                tree2.left = t2
                tree2.right = node.right
                tree2.w = tree2.left.w + tree2.right.w
            else:
                (t1, t2) = SPLIT(node.right, i - node.left.w)
                tree1.left = node.left
                tree1.right = t1
                tree1.w = tree1.left.w + tree1.right.w
                tree2 = t2
        else:
            tree1.string = node.string.substr(0, i)
            tree2.string = node.string.substr(i)
            tree1.w = i
            tree2.w = node.string.length - i

        return (tree1, tree2)


Пускай дано дерево:

![1](https://neerc.ifmo.ru/wiki/images/a/ac/Split1.png)

Тогда результатом выполнения операции split по индексу 16 будет:

![2](https://neerc.ifmo.ru/wiki/images/b/b1/Split2.png)

Заметим, что появляются лишние вершины, у которых есть только один потомок. От них можно легко избавиться, просто заменив их на этого потомка. Тогда результатом той же операции split будет:

![3](https://neerc.ifmo.ru/wiki/images/8/8d/Split3.png)

**Время работы операции split равно O(h).**

---

### 3.4. Вставка подстроки (insert)

Чтобы вставить строку s в позицию insertIndex, исходное дерево сначала разбивается на две части. Затем строка s превращается в Rope из одного листа, после чего все части объединяются обратно.

Псевдокод:

    function INSERT(node, insertIndex, s):
        (tree1, tree3) = SPLIT(node, insertIndex)
        tree2 = Node(s)
        return MERGE(MERGE(tree1, tree2), tree3)

**Так как операция использует split и merge, её сложность равна O(h).**

---

### 3.5. Удаление подстроки (delete)

Удаление подстроки с индексами [beginIndex, endIndex) выполняется с помощью двух операций split. Удаляемая часть просто не включается в итоговое дерево.

Псевдокод:

    function DELETE(node, beginIndex, endIndex):
            (tree1, tree2) = SPLIT(node, beginIndex)
            tree3 = SPLIT(tree2, endIndex - beginIndex).second
            return MERGE(tree1, tree3)

**Время работы операции delete также равно O(h).**

---

## 4. Балансировка

Если не выполнять балансировку, дерево может выродиться в цепочку, из-за чего высота h станет равной O(n). 
![](https://neerc.ifmo.ru/wiki/images/2/2a/Bamboo.png)
Чтобы этого избежать, Rope хранят как АВЛ-дерево и выполняют стандартные балансировки.

**Так как высота АВЛ-дерева равна O(log n) (это я не буду доказывать💀), операции get, split, insert, delete и merge имеют асимптотику O(log n).**

---

## 5. Практическое применение Rope

Структура данных Rope применяется в задачах, где требуется работать с большими строками и часто изменять их содержимое. Основные области применения:

- текстовые редакторы и IDE, где часто выполняются вставки и удаления в середине текста;
- обработка больших текстовых файлов, логов и документов;
- задачи, где требуется быстро разбивать строки на части и объединять их обратно без полного копирования.

---
### 5.1 Плюсы
- Быстрые вставки и удаления. Операции insert и delete выполняются за O(log n), в отличие от обычных строк, где такие операции могут занимать O(n).
- Эффективная работа с большими строками. Rope не требует копирования всей строки при каждом изменении.
- Удобная поддержка операций split и merge, которые сложно реализовать эффективно для обычных строк.
---
### 5.2 Минусы

- Сложная реализация. Rope значительно труднее реализовать и отлаживать, чем обычную строку или массив символов.
- Дополнительные затраты памяти. Хранение дерева и вспомогательных данных требует больше памяти, чем простая строка.
- Неэффективен для коротких строк. Если строки маленькие или редко изменяются, расходы Rope могут превышать выигрыш.

---

## 6. Итог

Rope — это структура данных, которая хорошо подходит для работы с большими и часто изменяемыми строками. Она позволяет выполнять основные операции за логарифмическое время за счёт использования бинарного дерева и балансировки. При этом Rope имеет более сложную реализацию и не всегда оправдан для простых задач, поэтому использовать его стоит только там, где это действительно необходимо.

## 7. Источники 
https://neerc.ifmo.ru/wiki/index.php?title=Rope

https://habr.com/ru/articles/144736/

https://en.wikipedia.org/wiki/Rope_(data_structure)